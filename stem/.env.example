# ----------------------------
# Stem defaults - set your user credentials here
# ----------------------------

STEM_CONTAINER_NAME=stem
STEM_HOSTNAME=stem

# Host port -> container 22
STEM_SSH_PORT=22222

# Container user
STEM_USER=dev
STEM_UID=1000
STEM_GID=1000

# Password fallback (keys should be primary)
STEM_PASSWORD=change-me

# Host path to a public key file (recommended: a *.pub file)
# Examples:
#   /home/you/.ssh/id_ed25519.pub
#   /home/you/.ssh/id_rsa.pub
STEM_AUTH_KEYS_PATH=/absolute/path/to/your/public_key.pub
