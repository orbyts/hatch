# -----------------------------
# container identity + ssh
# -----------------------------
STEM_CONTAINER_NAME=stem
STEM_HOSTNAME=stem
STEM_SSH_PORT=22222

# -----------------------------
# user mapping (container + host)
# -----------------------------
# Container username (recommended: keep as "dev" for a public template)
STEM_USER=dev

# Host username (optional, but nice to document)
HOST_USER=dev

# Match your host user's UID/GID so mounted volumes behave correctly
STEM_UID=1000
STEM_GID=1000

# -----------------------------
# host mount points
# -----------------------------
# Set this to the Dropbox folder path on the host machine
# Examples:
#   /home/<you>/Dropbox
#   /Users/<you>/Library/CloudStorage/Dropbox   (macOS)
DROPBOX_PATH_ON_HOST=/home/dev/Dropbox

# -----------------------------
# bootstrap / runtime knobs
# -----------------------------
# Used only to unlock the account if needed for SSH + PAM.
# Change this (or omit and let the container generate one).
STEM_PASSWORD=change-me

# If you want bootstrap to pre-install a uv-managed python version,
# set this to a numeric version. (Apogee default is auto-houdini; this overrides it.)
APOGEE_UV_DEFAULT_PY=3.11.7

# -----------------------------
# ssh authorized keys injection
# -----------------------------
# Path on the HOST to the public key you want to allow for SSH login
# Examples:
#   /home/<you>/.ssh/id_ed25519.pub
#   /home/<you>/.ssh/authorized_keys
STEM_AUTH_KEYS_PATH=/home/dev/.ssh/id_ed25519.pub

# -----------------------------
# bindu (dotfiles)
# -----------------------------
# Optional. Leave blank to skip bindu install.
# Use HTTPS to avoid needing SSH keys inside the container.
BINDU_REPO=
BINDU_REF=main

# Set to 1 only if you want to overwrite an existing ~/.config
BINDU_FORCE=0
